# Suggested Commands

**No project commands exist** — no build, test, lint, format, or package manager (docs-only repo). Do not waste effort searching for `package.json`, `Makefile`, CI scripts, etc. (none tracked).

Work = reading/editing Markdown under `docs/` + root `CLAUDE.md`/`README.md`.

System: Darwin (macOS, BSD userland). No project-specific command-form quirks worth noting; standard `git`/`grep`/`ls`/`find` apply.

Useful:
- `serena memories check` (from project root) — verify memory cross-references after editing memories.